targets:
  $default:
    builders:
      freezed:freezed:
        generate_for:
          include:
            - lib/model/**.dart
      json_serializable:
        options:
          field_rename: snake
