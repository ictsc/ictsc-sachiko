include: package:lint/analysis_options.yaml

linter:
  rules:
    prefer_single_quotes: true
    sort_pub_dependencies: false

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gr.dart"